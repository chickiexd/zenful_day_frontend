services:
  zenful_shopping_frontend:
    build: .
    container_name: zenful_day_frontend
    networks:
      - nginx_net
      - zenful_day
      - tunnel

networks:
  nginx_net:
    external: true
  tunnel:
    external: true
  zenful_day:
    external: true
